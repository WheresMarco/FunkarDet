<!DOCTYPE html>
<html>
<head>
  <title>Funkar det?</title>
  <meta name="viewport" content="width=device-width, user-scalable=no" />

  <%= stylesheet_link_tag    "application", media: "all", "data-turbolinks-track" => true %>
  <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  <%= csrf_meta_tags %>
</head>
<body>
  <div class="navbar navbar-fixed-top" role="navigation">
      <div class="container">
        <div class="navbar-header">
          <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
          </button>
          <% if @current_user %>
            <a class="navbar-brand" href="/"><%= @group.name %></a>
          <% else %>
            <a class="navbar-brand" href="/">Funkar det?</a>
          <% end %>
        </div>
        <div class="collapse navbar-collapse">
          <ul class="nav navbar-nav navbar-right">
            <% if @current_user %>
              <% if !@current_user.organizer %>
                <li><a href="#">Logged in as <%= @group_member.name %></a></li>
              <% else %>
                <li><%= link_to "New Group Date", new_group_group_date_path(@group) %></li>
                <li><%= link_to "New Member", new_group_group_member_path(@group) %></li>
                <li><%= link_to 'Edit Group', edit_group_path(@group) %></li>
              <% end %>
              <li><%= link_to "Log out", logout_path %></li>
            <% else %>
              <li><%= link_to "Sign Up", new_user_path %></li>
              <li><%= link_to "Sign In", login_path %></li>
            <% end %>
          </ul>
        </div><!--/.nav-collapse -->
      </div>
    </div>
  </div>

  <div class="container">
      <% flash.each do |type, message| %>
        <div class="alert flash alert-info <%= type %>" role="alert">
          <button type="button" class="close" data-dismiss="alert"><span aria-hidden="true">×</span><span class="sr-only">Close</span></button>
          <%= message %>
        </div>
      <% end %>

      <div class="row">
        <br />
        <%= yield %>
      </div>
</body>
</html>
